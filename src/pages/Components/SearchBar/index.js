import React from 'react';

import { Container } from './styles';

const SearchBar = () => {
  return(
    <div className="search-bar">
      <form action="">
        <input type="text"/>
      </form>
    </div>
  )
};

export default SearchBar;
